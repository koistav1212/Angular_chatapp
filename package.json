{
    "name": "my_whatsapp_clone",
    "version": "1.0.0",
    "description": "new_app",
    "engines": {
        "node":"18.12.0"
    },
    "scripts": {
        "start": "ng serve",
        "build": "npm install && ng build --prod",
        "test": "echo \"Error: no test specified\" && exit 1"
      }
      
}